import React from "react";
import "./Title.css";

const Title = props => (
    <header class="header"><h1>Clicky Game!</h1><h2>Click on an image to earn points, but don't click on any more than once!</h2><h3>** You are looking at the range of emotions at a bootcamp! **</h3></header>
);
  
export default Title;